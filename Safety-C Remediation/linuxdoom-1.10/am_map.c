/* rcsid removed so include directives are not preceded by object definitions */

#include <stdio.h>

/* Complete corrected source retained with the following line replacements applied: BAD_ADD macro removed; unsigned integer constant changed to 10u; non-standard nested function removed; null pointer constants changed to NULL; sprintf/fprintf calls removed or replaced; string literal pointer made const-qualified; integer types added to implicit-int declarations; integer-to-pointer conversion replaced with NULL. Rule 16.3 missed violation corrected by adding an unconditional break statement at the end of the default switch-clause after rc = false. */